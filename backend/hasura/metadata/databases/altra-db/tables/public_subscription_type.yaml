table:
  name: subscription_type
  schema: public
array_relationships:
- name: users
  using:
    foreign_key_constraint_on:
      column: subscription_id
      table:
        name: users
        schema: public
